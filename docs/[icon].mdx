---
title: ""
layout: "default"
---

export const CustomizeField = props => (
    <div className="w-full">
        <div className="flex items-center gap-1 text-gray-600 mb-1">
            <div className="flex items-center text-sm">
                {props.icon}
            </div>
            <div className="text-xs font-bold">
                <span>{props.title}</span>
            </div>
        </div>
        {props.children}
    </div>
);

export const SizeInput = props => (
    <input defaultValue="24" id={props.id} type="number" min="1" step="1" className="w-full block bg-white p-2 border border-gray-300 rounded-md text-sm" />
);

<div className="text-4xl font-bold mb-4 mt-8">
    <span id="iconName">{props.page.data.icon.name}</span>
</div>
<div className="flex flex-col md:flex-row gap-8 w-full mb-8">
    <div className="w-full">
        <div className="flex items-center justify-center w-full rounded-lg bg-gray-100 h-96 border border-gray-300">
            <div id="iconPreview" className="flex items-center text-gray-800" style={{fontSize: "12em"}}>
                <Icon icon={props.page.data.icon.name} />
            </div>
        </div>
    </div>
    <div className="w-full md:w-96 shrink-0">
        <div className="mb-6">
            <div className="mb-1 font-bold text-2xl">Download</div>
            <div className="mb-4">Download the icon as SVG image to use or edit it.</div>
            <a href={`./icons/${props.page.data.icon.name}.svg`} target="_blank" className="flex justify-center items-center w-full rounded-md bg-gray-900 hover:bg-gray-700 p-3 gap-2 no-underline">
                <div className="flex items-center text-2xl text-white">
                    <Icon icon="download" />
                </div>
                <div className="text-white font-bold">
                    <span>Download Icon</span>
                </div>
            </a>
        </div>
        <div className="">
            <div className="mb-1 font-bold text-2xl">Customize icon</div>
            <div className="mb-4">Set your custom size or color and download the icon as SVG or PNG image.</div>
            <div className="flex gap-2 flex-nowrap mb-4">
                <CustomizeField title="Width" icon={(<Icon icon="arrows-horizontal" />)}>
                    <SizeInput id="iconWidth" />
                </CustomizeField>
                <CustomizeField title="Height" icon={(<Icon icon="arrows-vertical" />)}>
                    <SizeInput id="iconHeight" />
                </CustomizeField>
                <CustomizeField title="Color" icon={(<Icon icon="palette" />)}>
                    <input id="iconColor" type="color" defaultValue="#000" className="w-full block bg-white p-1 border border-gray-300 rounded-md h-10 outline-0" />
                </CustomizeField>
            </div>
            <div className="flex flex-nowrap gap-2">
                <div id="downloadSvg" className="flex justify-center items-center w-full rounded-md bg-gray-900 hover:bg-gray-700 p-3 gap-2 cursor-pointer">
                    <div className="flex items-center text-2xl text-white">
                        <Icon icon="code" />
                    </div>
                    <div className="text-white font-bold">
                        <span>Get SVG</span>
                    </div>
                </div>
                <div id="downloadPng" className="flex justify-center items-center w-full rounded-md bg-gray-900 hover:bg-gray-700 p-3 gap-2 cursor-pointer">
                    <div className="flex items-center text-2xl text-white">
                        <Icon icon="image" />
                    </div>
                    <div className="text-white font-bold">
                        <span>Get PNG</span>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<Separator />

# Embed this icon within HTML

Copy and paste the following SVG code into your HTML file where you want the icon to appear. Then, you can customize the SVG attributes, such as size or color, using inline styling or CSS classes.

<CodeBlock language="html">
{`<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" width="1em" height="1em">
    <path fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="${props.page.data.icon.path}" />
</svg>`}
</CodeBlock>

# Using as a React Component

You can integrate this icon in your React application using our React icons components. First install the package using **YARN** or **NPM**:

<CodeBlock language="shell">{`$ yarn add @josemi-icons/react`}</CodeBlock>

Now you can import this icon in your React application

<CodeBlock language="jsx">{`import {${props.page.data.icon.reactComponentName}} from "@josemi-icons/react";`}</CodeBlock>

Alternatively, if you only need this icon jou can just copy and paste the following component into your React application:

<CodeBlock language="jsx">
{`const ${props.page.data.icon.reactComponentName} = () => (
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" width="1em" height="1em">
        <path fill="none" stroke="currentColor" strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="${props.page.data.icon.path}" />
    </svg>
);`}
</CodeBlock>

You can now use the icon component in your React application like any other React component.


<script type="text/javascript" src="./customize.js" />
